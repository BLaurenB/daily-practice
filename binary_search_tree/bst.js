// My attempts at a Binary Search Tree
// I think that adding and inserting are the same function, so I'll just do that.


export default class Bst {
  constructor() {
    this.root = null
  }

  insert(num) {
    // is there a root yet?

    // there is a root. Compare the passed num to the left and right.
      // if left === null
      // if right === null
      // if n < left
      // if n > right
      // if left < num < right
  }


}
